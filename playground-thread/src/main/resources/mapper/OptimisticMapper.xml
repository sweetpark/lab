<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.playground.thread.mapper.OptimisticMapper">
    <select id="selectById" resultType="com.example.playground.thread.entity.Product">
        SELECT id, quantity, version
        FROM product
        WHERE id = #{id}
    </select>

    <update id="updateWithVersion">
        UPDATE product
        SET quantity = quantity - 1,
            version = version + 1
        WHERE id = #{id}
          AND version = #{version}
    </update>

</mapper>